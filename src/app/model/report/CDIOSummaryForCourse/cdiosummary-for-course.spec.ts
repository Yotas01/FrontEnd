import { CDIOSummaryForCourse } from './cdiosummary-for-course';

describe('CDIOSummaryForCourse', () => {
  it('should create an instance', () => {
    expect(new CDIOSummaryForCourse()).toBeTruthy();
  });
});
