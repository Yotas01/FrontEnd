import { PerformanceIndicator } from './performance-indicator';

describe('PerformanceIndicator', () => {
  it('should create an instance', () => {
    expect(new PerformanceIndicator()).toBeTruthy();
  });
});
