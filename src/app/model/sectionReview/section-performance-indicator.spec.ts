import { SectionPerformanceIndicator } from './section-performance-indicator';

describe('SectionPerformanceIndicator', () => {
  it('should create an instance', () => {
    expect(new SectionPerformanceIndicator()).toBeTruthy();
  });
});
