import { SectionAssessmentTool } from './section-assessment-tool';

describe('SectionAssessmentTool', () => {
  it('should create an instance', () => {
    expect(new SectionAssessmentTool()).toBeTruthy();
  });
});
